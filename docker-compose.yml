services:
  wildlife-summary:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ig_data:/data      # persists Instagram session + downloaded media

volumes:
  ig_data:
